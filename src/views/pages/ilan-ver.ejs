<main class="container mx-auto px-6 py-8 max-w-4xl">
<div class="flex items-center text-sm text-slate-500 mb-6 gap-1">
<a href="/" class="hover:text-primary">Ana Sayfa</a><span>/</span><span class="text-slate-800 font-medium">İlan Ver</span>
</div>

<div class="bg-white rounded-2xl border border-slate-100 p-8">
<h1 class="text-2xl font-bold text-slate-900 mb-2">Araç İlanı Oluştur</h1>
<p class="text-slate-500 mb-8">Aracınızın bilgilerini girerek hemen ilan verebilirsiniz.</p>

<form action="/ilan/ver" method="POST" class="space-y-6">
<!-- Marka / Model -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Marka <span class="text-red-500">*</span></label>
<select name="brand_id" id="brand_id" required class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary">
<option value="">Marka seçin</option>
<% brands.forEach(b => { %><option value="<%= b.id %>"><%= b.name %></option><% }) %>
</select>
</div>
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Model <span class="text-red-500">*</span></label>
<select name="model_id" id="model_id" required class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary">
<option value="">Önce marka seçin</option>
</select>
</div>
</div>

<!-- İlan Başlığı -->
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">İlan Başlığı (opsiyonel)</label>
<input name="title" type="text" placeholder="Otomatik oluşturulur (ör: 2020 BMW 320i)" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>

<!-- Yıl / KM / Fiyat -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-4">
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Model Yılı <span class="text-red-500">*</span></label>
<input name="year" type="number" min="1970" max="2025" required placeholder="2020" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Kilometre</label>
<input name="km" type="number" min="0" placeholder="50000" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Fiyat (₺) <span class="text-red-500">*</span></label>
<input name="price" type="number" min="1" required placeholder="500000" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>
</div>

<!-- Yakıt / Vites / HP -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-4">
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Yakıt Tipi</label>
<select name="fuel_type" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary">
<option value="">Seçin</option>
<option value="benzin">Benzin</option>
<option value="dizel">Dizel</option>
<option value="elektrik">Elektrik</option>
<option value="hibrit">Hibrit</option>
<option value="lpg">LPG</option>
</select>
</div>
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Vites</label>
<select name="transmission" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary">
<option value="">Seçin</option>
<option value="otomatik">Otomatik</option>
<option value="manuel">Manuel</option>
<option value="yari_otomatik">Yarı Otomatik</option>
</select>
</div>
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Beygir Gücü</label>
<input name="hp" type="number" placeholder="150" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>
</div>

<!-- Renk / Şehir -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Renk</label>
<input name="color" type="text" placeholder="Beyaz" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Şehir</label>
<input name="city" type="text" placeholder="İstanbul" class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"/>
</div>
</div>

<!-- Açıklama -->
<div>
<label class="block text-sm font-semibold text-slate-700 mb-1">Açıklama</label>
<textarea name="description" rows="5" placeholder="Aracınız hakkında detaylı bilgi verin..." class="w-full rounded-xl border-slate-200 focus:border-primary focus:ring-primary"></textarea>
</div>

<button type="submit" class="w-full bg-primary text-white font-bold py-4 rounded-xl hover:bg-primary/90 transition-all text-lg flex items-center justify-center gap-2">
<span class="material-symbols-outlined">publish</span> İlanı Yayınla
</button>
</form>
</div>
</main>

<script>
document.getElementById('brand_id').addEventListener('change', async function() {
  const modelSelect = document.getElementById('model_id');
  modelSelect.innerHTML = '<option value="">Yükleniyor...</option>';
  if (!this.value) { modelSelect.innerHTML = '<option value="">Önce marka seçin</option>'; return; }
  try {
    const res = await fetch('/api/models/' + this.value);
    const models = await res.json();
    modelSelect.innerHTML = '<option value="">Model seçin</option>' + models.map(m => `<option value="${m.id}">${m.name}</option>`).join('');
  } catch(e) { modelSelect.innerHTML = '<option value="">Hata oluştu</option>'; }
});
</script>
